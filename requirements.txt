# The agent runtime's dependencies: the AgentCore Runtime container image,
# where the Strands agent and the models actually run.
#
# The Lambdas use requirements-lambda.txt instead. They never load a model, so
# bundling these into the Lambda zip adds tens of megabytes of code that is
# never imported, against a 50MB limit.
boto3>=1.35
strands-agents>=0.1
bedrock-agentcore>=0.1
